<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <corners android:radius="12dp"/> <!-- Bordes redondeados -->
    <solid android:color="@android:color/white"/> <!-- Color de fondo blanco -->
    <stroke
        android:width="1dp"
        android:color="@android:color/transparent"/> <!-- Borde gris oscuro -->
    <padding
        android:left="8dp"
        android:right="8dp"
        android:top="8dp"
        android:bottom="8dp"/> <!-- Espaciado interno -->
</shape>
